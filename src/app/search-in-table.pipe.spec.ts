import { SearchInTablePipe } from './search-in-table.pipe';

describe('SearchInTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInTablePipe();
    expect(pipe).toBeTruthy();
  });
});
